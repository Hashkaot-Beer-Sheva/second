import{defineConfig}from'vite';import fs from'node:fs/promises';import path from'node:path';import{parseCSV,parseCSVFromRow,parseMatrix,matrixObjects,buildData}from'./functions/api/data.js';
const files=['Unified Attempt1.csv','EasyRent Prod - Apartments (1).csv','Hashkaot Renters zehavit (1).csv','Beer Sheva Monthly Report- Purchases.csv','New Occupancy  - Occupancy.csv','building-coordinates (4) (1).csv'];
export default defineConfig({plugins:[{name:'local-property-data',configureServer(server){server.middlewares.use('/api/local-data',async(_req,res)=>{try{const t=[];for(const n of files)t.push(await fs.readFile(path.resolve('local-data','SPREADSHEETS',n),'utf8'));const p=t.map(parseCSV);p[2]=parseCSVFromRow(t[2],1);const d=buildData({unified:p[0],easy:p[1],renters:p[2],purchases:p[3],occupancy:parseMatrix(t[4]),coordinates:p[5]});d.raw.coordinates=p[5];d.raw.occupancy=matrixObjects(parseMatrix(t[4]));res.setHeader('content-type','application/json');res.end(JSON.stringify(d))}catch(e){res.statusCode=500;res.end(JSON.stringify({error:e.message}))}})}}]});
